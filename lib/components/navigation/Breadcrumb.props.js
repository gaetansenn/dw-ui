export default {
  /**
   * Items to display
   * Array of Object with { to: 'link', href: 'link', target: 'used with href', label: 'to display (required)' }
   * Or Array of String
   */
  items: {
    type: Array,
    required: true
  }
}
